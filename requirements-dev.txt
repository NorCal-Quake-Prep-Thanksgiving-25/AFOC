bandit==1.7.8
black==24.8.0
fastapi==0.120.0  # Security: matches runtime pin with Starlette patch requirements.
httpx==0.27.0
mypy==1.13.0
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.23.8
sqlalchemy==2.0.31
ruff==0.6.9
uvicorn==0.30.6
cryptography==44.0.1  # Security: patched dependency removes GHSA-79v4-65xg-pq4g exposure.
starlette==0.47.2  # Security: aligns dev stack with patched Starlette release.
pip-audit==2.7.3  # Security: ensures dependency advisories are enforced in CI.
safety==3.2.4  # Security: redundant vulnerability scan catches ecosystem issues.
