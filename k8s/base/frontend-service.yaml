apiVersion: v1
kind: Service
metadata:
  name: afoc-frontend
  namespace: afoc
  labels:
    app: afoc-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: afoc-frontend
