apiVersion: v1
kind: Service
metadata:
  name: afoc-services
  namespace: afoc
  labels:
    app: afoc-services
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: afoc-services
