apiVersion: v1
kind: Service
metadata:
  name: afoc-ml-engine
  namespace: afoc
  labels:
    app: afoc-ml-engine
    component: ml
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: afoc-ml-engine
